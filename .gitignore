CLAUDE.md
node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Local configuration. `.env.example` is tracked as the template; the real
# values stay out of the repo. The WhatsApp number in `.env` today is public,
# but this is where a database URL or payment key would land at the commerce
# phase, and those must never be committed.
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Local project notes for Claude, not meant to be shared in the repo
CLAUDE.md
